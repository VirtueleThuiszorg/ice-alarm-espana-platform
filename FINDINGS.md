# FINDINGS.md — public-route page audit

Generated by the per-page audit harness (`npm run audit:pages`, LAUNCH_SCOPE.md §8)
against the **production build** (`vite build` → `vite preview`) with a placeholder
(anon, non-functional) Supabase. First full run: **2026-07-22**.

The harness runs **green-or-annotated**: every failure below is registered in
`KNOWN_ISSUES` in `e2e/public.spec.ts` and skipped via `test.fixme()` with a reason,
so a NEW failure (a regression) is what turns the suite red — a known defect never
reads as passing (GOALS.md G5). Remove the matching `KNOWN_ISSUES` entry when a
defect is fixed and its check goes green.

## What passed on every public route
- **No dead anchors** (`href="#"` / empty) — 14/14 routes clean.
- **Internal links resolve** to a declared route in `src/App.tsx` — 14/14 clean.
- **No forbidden brand strings** (`icealarm`, `icehealthsync`, `ICE Alarm`) in the
  rendered production output — 14/14 clean. *(Note: stale strings still exist in
  source/i18n JSON and non-public surfaces — that is Prompt 5's brand-sweep scope;
  they simply do not render on these public pages.)*
- **No no-op buttons** detected on the routes exercised.

## Failures (known defects → `test.fixme` with reason)

> **✅ F1–F8 RESOLVED in the AI-strip PR (`feat/public-ai-strip`).** All keys below
> were added to `en.json`, `es.json`, and `nl.json`; their render checks now run and
> pass. NL is now a wired language (`supportedLngs: ["en","es","nl"]`, full `nl.json`).
> The table is kept for the record.

These were **missing i18n translation keys** — each absent from `en.json` (the
fallback), so it rendered as the raw key and the same key was reported for `en`,
`es`, and `nl`.

| # | Route | Check | Missing keys (now added) |
|---|---|---|---|
| F1 | `/` (landing) | render (en/es/nl) | `help.title` |
| F2 | `/pricing` | render (en/es/nl) | `pricing.oneTime`, `pricing.pendant`, `pricing.shipping`, `pricing.registration`, `landing.save` |
| F3 | `/help` | render (en/es/nl) | `help.heading`, `help.subheading`, `help.searchPlaceholder`, `help.allCategories`, `help.userGuide`, `help.faq`, `help.general`, `help.device`, `help.footer`, `help.contactUs` |
| F4 | `/terms` | render (en/es/nl) | `legal.footer.termsOfService`, `legal.footer.privacyPolicy` |
| F5 | `/privacy` | render (en/es/nl) | `legal.footer.termsOfService`, `legal.footer.privacyPolicy` |
| F6 | `/login` | render (en/es/nl) | `validation.passwordMin` |
| F7 | `/partner` | render (en/es/nl) | `partnerOnboarding.howItWorksTitle`, `.step1Title`, `.step1Desc`, `.step2Title`, `.step2Desc`, `.step3Title`, `.step3Desc`, `.commissionTitle`, `.commBase`, `.comm10`, `.comm20`, `.region`, `.selectRegion`, `.howHeard`, `.selectHowHeard` |
| F8 | `/partner/login` | render (en/es/nl) | `partnerLogin.title`, `partnerLogin.subtitle` |

**Fix:** add the missing keys to `src/i18n/locales/en.json` **and** `es.json` (and
`nl.json` once NL is wired). These are static UI labels, not data-driven content.

## Button-check false positives (B1) — heuristic limitation, NOT dead buttons

The no-op-button heuristic flagged buttons on `/contact`, `/partner`, `/help`, and
`/join`. Each was investigated in source and has a **real handler** — the effect is
simply not DOM-observable under the audit's anon/empty backend. These are annotated
`test.fixme` (`… :: dead-buttons`) and are **not** defects:

| Route | Button(s) | Why it's a false positive |
|---|---|---|
| `/how-it-works` | "What if it's 3am?" | Radix `AccordionTrigger` — real; the expand animation isn't caught by the click-snapshot heuristic's timing. |
| `/pendant` | FAQ questions ×3 | Radix `AccordionTrigger`s on the pendant page (routed to `PendantPage` at launch) — same heuristic limitation. |
| `/contact` | "Send Message" | `<form onSubmit={handleSubmit}>` — needs filled fields + a live provider to produce a visible result. |
| `/partner` | "Register Your Interest" | Partner-onboarding `onSubmit` handler — same as above. |
| `/help` | "general", "device" | Category filters (`setSelectedCategory`) over an article list that is empty because the backend is a placeholder — filtering nothing changes nothing. |
| `/join` | "Plan" step nav, Back on step 1 | Wizard nav handlers that are legitimately no-ops for the *current* step. |

The Isabella chat widget's "Call and Speak" (seen on `/how-it-works` and every page)
is now allowlisted — the widget is out of audit scope (LAUNCH_SCOPE §2).

**Takeaway:** the no-op detector is a best-effort heuristic. It is reliable for
plain navigation/toggle buttons (it passed cleanly on 9/14 routes) but cannot judge
form-submit or data-dependent buttons without a live backend and seeded input. A
future improvement is a seeded-backend fixture so these can be asserted for real
rather than skipped.

## Cross-cutting notes (not per-route failures)

- **NL is not implemented.** `src/i18n/index.ts` sets `supportedLngs: ["en","es"]`
  and there is no `nl.json`, yet LAUNCH_SCOPE.md §6 requires EN/ES/NL at launch.
  Requesting `nl` **silently falls back to English** — so the `nl` render checks do
  not fail *extra* today (they mirror en), but NL coverage is genuinely absent. This
  is Stage 4 / §6 work (add `nl.json`, extend `supportedLngs`). Once NL is wired,
  these checks will begin asserting real NL coverage.
- **Provider-dependent content is not asserted green.** Data-driven pages (blog
  posts, FAQ articles, testimonials) render their loading/empty states because the
  audit uses a non-functional anon Supabase. The harness only asserts static
  structure/labels; live-data assertions are intentionally out of scope here and are
  marked so, never falsely green (GOALS.md G5).

## Harness scope / limitations
- Public routes only (this PR). Authenticated surfaces (admin, call-centre, client
  dashboard, partner dashboard) need a seeded/authenticated fixture — follow-up.
- The no-op-button check is a **conservative heuristic** (clicks each visible button
  and looks for an observable change: navigation, dialog, toast, `aria-expanded`, or
  DOM mutation). It under-reports rather than flag genuinely-interactive controls.
- The suite runs `workers: 1` and the app cold-boots in ~13s per fresh context, so a
  full pass takes ~20 min. Acceptable for a CI gate; not a per-commit fast check.
