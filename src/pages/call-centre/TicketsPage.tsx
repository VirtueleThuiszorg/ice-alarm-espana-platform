import TicketsPage from "@/pages/admin/TicketsPage";

// Re-export admin TicketsPage for call centre use — one shared implementation
// with full ticket CRUD: create, comment, status, priority, assign, resolve, close.
export default TicketsPage;
